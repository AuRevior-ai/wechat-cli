# WeChat CLI Current Project State

Updated: 2026-08-15

## Repository-verifiable baseline

- Product: `wechat-cli-web`
- Application version: `0.6.0` on the Board 7 lineage now present in canonical remote `main`; the accepted Board 6 lineage remains historically identifiable at `0.5.1`.
- Launcher version: `0.2.0` on the Board 7 lineage now present in canonical remote `main`; the accepted Board 6 lineage remains historically identifiable at `0.1.0`.
- Source repository: `AuRevior-ai/wechat-cli`
- Historical frozen main baseline: `a579a25cb7f16e6fdf88d618252b4a5cbffef53d`. This remains the frozen pre-integration reference used by earlier Board 4/5/6 evidence; it is no longer canonical `main`.
- Current canonical remote main: `fd29a2a7b00ded303c9c6ee8bdab8b1f2bbccc75`, the history-preserving merge commit for Strong Authorization PR #3. Its first parent is whitespace-repair merge `a47bb21526d1d6e2fc3bc66567807943a488df51`; primary Board 7 integration remains historically anchored at PR #1 merge `f63eb76b596080cb68b2285a8d0b0cc8e413a9a4`.
- Retained local main checkout: `D:\use_as_desktop\Wechat__CLI\wechat-cli` remains physically checked out at historical frozen SHA `a579a25cb7f16e6fdf88d618252b4a5cbffef53d` with its pre-existing `?? NUL`. It is intentionally not being fast-forwarded or cleaned as part of Board 7 and must not be mistaken for canonical remote `main`.
- Task 5 implementation baseline: `84b8a99` (`feat: add 0.5.1 update-only packaging`)
- External-memory governance design: `5310630`
- Current active Board 7 branch: `board7/production-infrastructure-provision`, based exactly on canonical remote `main=fd29a2a7b00ded303c9c6ee8bdab8b1f2bbccc75`. Whitespace-repair PR #2 and Strong Authorization PR #3 are both history-preserving merged with canonical-main CI PASS. Strong Authorization is active. B7-G3 Production Infrastructure Provision is in progress: clean-room D1 `wechat-cli-license-production` (ID `011b3c26-bbe6-4bb7-8af7-39f1e6d46932`) and R2 buckets `wechat-cli-releases-production` / `wechat-cli-diagnostics-production` are created, migrations `0001`–`0008` are fully applied, and fresh production business/identity/audit row counts are zero. Production Access applications are not yet created because the existing Wrangler OAuth authority was rejected for Access app creation with HTTP 403 / code `1010`; production Worker, runtime Secrets/private keys, principals, licenses and releases remain undeployed/uncreated. Commercial Authenticode remains deferred optional Public / Formal Distribution hardening.

## Current program position

The authorized-update program has completed board 4, “first test license and test release.” Board 5 design/spec and the seven-task implementation plan are documented and approved in the isolated branch `board5/windows-staging-e2e`, based on the frozen Board 4 snapshot `8c7464f`. **Board 5 Local implementation Task 1–3, Bootstrap build Task 4, stable-license creation, isolated bootstrap activation, the stable 0.5.0→0.5.1 real Windows E2E, Task 6 Part A Offline acceptance, the separately approved beta-license creation gate, and RollbackSandbox beta activation/alignment are complete.** The private `v0.5.1` release visibility blocker was resolved under an explicit publish gate: Release ID `367353041` is now `draft=false`, `prerelease=false`, `make_latest=false`, and `refs/tags/v0.5.1` points to release-repository commit `2b9fa385b86df83f7968239a1029d4d59f020027`; all three approved assets remained unchanged. A post-publish Board 5 range probe returned HTTP 206 with `Content-Range: bytes 0-0/14268929`. The real installed 0.5.0 application then triggered the installed Launcher update path while the old 0.5.0 session stayed alive throughout download. The downloaded ZIP verified at size/SHA-256 `14268929` / `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`; manifest SHA-256 `be111f0a786821319fb96f82f1748b4a75003b7c5fb296c48ec87b3355a3fd62` verified under `release-key-staging-01`; safe extraction produced the expected 0.5.1 EXE with SHA-256 `dbbdf8ec70ddcf8d36145f099b0cb4ce3292c61e205c3ac7ae4db01fbc8907d1`. One controlled restart through the installed Launcher committed the update transaction and real loopback health now returns `version=0.5.1`, `build_id=staging-051-20260808.1`, `status=ok`, `license_session_valid=true`; `current.json` is current `0.5.1`, previous `0.5.0`, with the approved manifest SHA, `pending-update.json` is absent, and both version directories remain present. Final Worker/GitHub read-only reconcile found `rel_staging_050/051` metadata unchanged; `v0.5.0` remains its original Draft and `v0.5.1` differs only by the authorized publish/tag mutation. Board 5 Offline Part A, beta-license creation, RollbackSandbox beta activation/alignment, Task 6 Part C local fault preparation, fault publish/register, and fault-enable are complete. The fault visibility blocker was separately resolved by publishing Release ID `368572125` as a private prerelease with `make_latest=false`; tag `v0.5.2-board5bad.1` points to release-repository commit `2b9fa385b86df83f7968239a1029d4d59f020027`, all three frozen fault assets remained unchanged, and a post-publish Worker one-byte probe returned HTTP 206 with `Content-Range: bytes 0-0/14268937`. The subsequent real rollback attempt successfully downloaded, verified, and extracted the fault candidate and drove the update transaction to `rolled_back` with failure reason `application health version does not match the update`; current restored to 0.5.1 and the failed registry recorded `0.5.2-board5bad.1`. However, real acceptance failed because the PyInstaller candidate child survived after its Popen parent exited, continued holding port 18787, and caused restored-health false-positive behavior. The abnormal RollbackSandbox processes were explicitly stopped and the stable sandbox was restored to a verified 0.5.1 listener with `license_session_valid=true`. The process-tree repair is now implemented at `29aba6bc0c8469dc8b5dc512d6831c5385246431` and has passed fresh real re-acceptance in a separate `RollbackRepairSandbox`: the repaired Launcher terminated the candidate tree, the sole restored listener belonged to `versions/0.5.1/wechat-cli.exe`, health returned `0.5.1 / staging-051-20260808.1 / ok / license_session_valid=true`, the transaction remained `rolled_back` for the expected version mismatch, and the local failed registry recorded candidate version + frozen manifest hash. A subsequent beta check sent `failed_versions=[0.5.2-board5bad.1]` and returned no update, proving **version-level server suppression**. The repair sandbox was stopped and stable 0.5.1 was restored with a fresh valid listener/session. The separately approved final fault-disable gate is now complete: one Admin API PATCH changed only `rel_board5_bad_052_01` to `enabled=false` / `paused=true` while leaving rollout at 100; GitHub prerelease/tag/assets and stable `rel_staging_050/051` remained unchanged. Board 5 **accepted complete** on 2026-08-12. Under the approved Cloud Cleanup Gate, both Board 5 stable/beta licenses are `revoked` at revision 2 with `active_devices=0`, and their two associated Board 5 test-device rows are `unbound`; fresh post-cleanup readback confirms JD25 and `rel_staging_050/051` were unchanged and the fault release remains disabled/paused. Fresh final verification passed Python 529 run / 2 expected skips / 0 failures, Worker typecheck, Vitest 21/21, immutable Git/worktree checks, fixed 0.5.0 ZIP verification, Worker release reconcile and GitHub release/asset/tag reconcile. The final Board 5 acceptance report is `docs/deployment/2026-08-12-board-5-windows-e2e-report.md`. Local physical cleanup remains optional and separately gated. Board 6 is **accepted complete** under the approved Private / Controlled Distribution profile. **B6-G0 source integration is complete through `c1d045895a044dbb4c9998a787c77775654074fa`; B6-G1 Update Trust Local Gate is complete through `bdc98afc0d945c4c86f1e3b21686d2fe798ccdd1`; B6-G2 Admin & Data Security Local Gate is complete through `e0c91df`; B6-G3 Windows Integrity Local Gate is complete through implementation HEAD `1a07447` with pre-staging audit `d73cf3f`. B6-G4 Staging Infrastructure Gate is complete: dedicated staging release R2, D1 migrations `0004`–`0007`, all seven Board 6 `_V1` Secret names, the Access-protected staging admin hostname/verifier boundary, and staging Worker deployment are live. B6-G5 Staging Behavior Acceptance is also accepted complete with evidence `docs/superpowers/governance/2026-08-14-board-6-staging-behavior-acceptance.md`; live acceptance repaired Access JWKS redirect handling, R2 full-download status, Python channel-mismatch error mapping, and diagnostics product User-Agent behavior, then reaccepted channel authority, exact failed-candidate suppression, immutable GitHub provenance + R2 runtime distribution, Access-backed 30-minute admin sessions, Origin/rate controls, and diagnostics retention/delete behavior. B6-G6 Phase A provider-neutral readiness repair/research is complete locally at `9f4ad0f` + `e9cb67b` with provider decision research retained at `docs/superpowers/governance/2026-08-14-board-6-code-signing-provider-decision.md`; dormant optional SSL.com adapter code is at `50e7074`. Under the approved Private / Controlled Distribution amendment, real commercial Authenticode provider procurement/KYC/payment/key provisioning/signing is explicitly deferred and is not a current launch or Board 6 closure blocker. B6-G7 Staging Key Rotation Drill is accepted complete with evidence `docs/superpowers/governance/2026-08-14-board-6-key-rotation-preflight.md`; B6-G8 final closure is accepted complete with report `docs/deployment/2026-08-12-board-6-security-delivery-preparation-report.md`; Board 7 B7-G0 design/spec and implementation plan are complete, B7-G1 Local Production Hardening is accepted complete locally through `b8551d0`, and the primary B7-G2 lineage is now history-preserving merged into canonical remote `main=f63eb76b596080cb68b2285a8d0b0cc8e413a9a4`; remaining B7-G2 work is the already-pushed whitespace repair plus approved Strong Authorization amendment integration before B7-G3.** B6-G3 consists of `4b7fbfe` exact pywebview pin + preload adapter, `a1dc6bd` embedded Launcher deployment trust profile, `9b54710` Authenticode/runtime/signing-provider boundary, `8d7493e` production-capable signed installer target, and `1a07447` fail-closed Worker deployment preflight. Fresh B6-G3 verification passed Python 607 run / 2 expected skips / 0 failures, Worker typecheck, Vitest 13 files / 89 tests, implementation diff check, static trust-boundary checks and targeted non-test sensitive-shape scanning. No cloud/staging/production mutation or real signing occurred. The seven Board 6 `_V1` staging Secret names are now provisioned while the retained legacy compatibility names remain present; production D1 remains a replacement placeholder and production routes remain intentionally empty, so production remains fail closed. B6-G4 closure evidence is `docs/superpowers/governance/2026-08-14-board-6-staging-infrastructure-gate.md`. Fresh post-deploy reconcile confirms both staging health origins return 200, the Access-protected admin login start returns 302 on the custom domain and 403 on workers.dev, D1 has no pending migration, the 15 staging Secret names are present, and all historical Board 5 release rows are unchanged. Historical frozen main baseline remains `a579a25cb7f16e6fdf88d618252b4a5cbffef53d` for Board 4/5/6 evidence, and the retained local main checkout still physically sits there with its existing `?? NUL`; canonical remote main is now `a47bb21526d1d6e2fc3bc66567807943a488df51`. Board 5 accepted-complete evidence remains frozen at `67d3dec0fd1c4a02c87be1ab79c4f78ea63f49d6`.

## Board 6 / Board 7 canonical execution status

- Board 5: **accepted complete**; final acceptance report is `docs/deployment/2026-08-12-board-5-windows-e2e-report.md`.
- Board 6: **accepted complete** on `board6/security-delivery-preparation` under the Private / Controlled Distribution profile; final acceptance report is `docs/deployment/2026-08-12-board-6-security-delivery-preparation-report.md`.
- Board 7 B7-G0: **design/spec + implementation plan complete locally**. Design: `docs/superpowers/specs/2026-08-15-board-7-production-automation-controlled-launch-design.md`; plan: `docs/superpowers/plans/2026-08-15-board-7-production-automation-controlled-launch.md`.
- Board 7 B7-G1 Local Production Hardening: **accepted complete locally** through implementation HEAD `b8551d05677ea5f5fccaf842c0214e49c70eb638`. Audit: `docs/superpowers/governance/2026-08-15-board-7-local-production-hardening-audit.md`. Fresh closure verification: Python 687 run / 2 expected skips / 0 failures; Worker typecheck PASS; Vitest 18 files / 130 tests PASS; deployment/workflow/release/security focused 58/58 PASS; workflow policy PASS; tracked sensitive-value scan PASS; `git diff --check` PASS.
- Board 7 B7-G2 Source Integration: **accepted complete**. Primary Board 7 PR #1 merged history-preserving at `f63eb76b596080cb68b2285a8d0b0cc8e413a9a4`; exact whitespace repair `9185f5feb0c558b5dd8c76c3f18df43188e59fcf` passed branch/PR CI and merged through PR #2 at `a47bb21526d1d6e2fc3bc66567807943a488df51`; Strong Authorization PR #3 then merged history-preserving as canonical remote `main=fd29a2a7b00ded303c9c6ee8bdab8b1f2bbccc75`. Canonical-main CI for `fd29a2a...` is PASS and fresh remote readback matched exactly.
- Board 7 B7-G3 Production Infrastructure Provision: **in progress** from exact canonical source `fd29a2a...`. Inventory/evidence: `docs/superpowers/governance/2026-08-15-board-7-production-inventory.md`. Read-only collision preflight passed; clean-room D1 ID `011b3c26-bbe6-4bb7-8af7-39f1e6d46932` and both exact production R2 buckets are now live; migrations `0001`–`0008` are complete with no pending migration; fresh business/identity/audit counts are all zero with no staging import. Exact API/Admin origins, Zero Trust issuer/JWKS, D1 ID and the two future custom-domain route declarations are prepared locally. Human/automation Access AUD/automation identity remain fail-closed placeholders because Access app creation through the current Wrangler OAuth authority was rejected with HTTP 403 / code `1010`. Production application Worker remains undeployed and no production Secret/private key/principal/license/release exists yet. The Strong Authorization Envelope remains active and Public / Formal Authenticode remains out of scope.
- B6-G0 Source Integration Gate: **complete**. The lineage started from exact frozen main `a579a25...`, selectively integrated only the audited Board 5 product candidates, preserved Board 5 evidence history, and completed the generic packaging-boundary cleanup at `c1d0458`.
- B6-G0 provenance: `docs/superpowers/governance/2026-08-12-board-6-source-integration-provenance.md`.
- Board 6 approved design/spec and implementation plan are now tracked in this Board 6 branch. Their approved seed SHA-256 values remain frozen in provenance; the exact seed files remain retained and untracked in the Board 5 evidence worktree pending any separately authorized cleanup.
- B6-G1 Update Trust Local Gate: **complete** through `bdc98afc0d945c4c86f1e3b21686d2fe798ccdd1`. Commits: `a23b6ff` channel authority, `988a504` exact failed-release identity/version immutability, `bdc98af` local R2 distribution/lifecycle model.
- B6-G1 fresh verification: Python 510 run / 2 expected skips / 0 failures; Worker typecheck PASS; Vitest 5 files / 40 tests PASS; `git diff e25f2ae..bdc98af --check` PASS; targeted non-test sensitive-shape scan 0 target matches; implementation worktree clean before governance edits.
- B6-G2 Admin & Data Security Local Gate: **complete** through `e0c91df`. Task 5–8 local code/schema/TDD are complete; no Access cloud configuration, staging migration/deploy, or real Secret mutation was performed.
- B6-G3 Windows Integrity Local Gate: **complete** through implementation HEAD `1a074472360907be10d336729c3c28e0584b00f3`; pre-staging security audit is `docs/superpowers/governance/2026-08-13-board-6-pre-staging-security-audit.md` at `d73cf3f`.
- B6-G3 fresh verification: Python 607 run / 2 expected skips / 0 failures; Worker typecheck PASS; Vitest 13 files / 89 tests PASS; `git diff 6b9e8b7..1a07447 --check` PASS; static trust-boundary audit and targeted non-test sensitive-shape scan PASS.
- B6-G4 Staging Infrastructure Gate: **complete**. Evidence: `docs/superpowers/governance/2026-08-14-board-6-staging-infrastructure-gate.md`; staging Worker Version ID `14a19ea3-5a96-408b-a4e3-0a8d8e4ebe2c`; R2/D1/Secret/Access/custom-domain/deploy reconcile PASS.
- B6-G5 Staging Behavior Acceptance Gate: **accepted complete**. Evidence: `docs/superpowers/governance/2026-08-14-board-6-staging-behavior-acceptance.md`. Current staging Worker Version ID after G5 repairs: `6f2aad56-12cb-4d8e-8af5-9dceefbe1a49`; fresh health is 200/staging. Final staging state: G5 release disabled/paused; both disposable G5 licenses revoked; diagnostics row deleted; G5 admin sessions and principal revoked with zero active sessions; immutable GitHub prerelease/tag and exact R2 package retained as provenance/transport evidence. Two historical G5 device rows still report `active` even though their parent licenses are revoked; this row-level cleanup residual is explicitly documented and is not an active authorization path. Operational handoff for future disposable acceptance cleanup: prefer `unbind disposable devices -> revoke disposable license`; do not reproduce the G5 ordering that left revoked-parent rows behind. Whether to add a dedicated revoked-parent safe-cleanup product capability is deferred to a separate design/authorization decision and is not part of code-signing work.
- B6-G6 Phase A provider-neutral readiness repair + provider research: **complete locally; real commercial signing itself is not completed and is now explicitly deferred**. Entry audit: `docs/superpowers/governance/2026-08-14-board-6-code-signing-entry-readiness.md`; provider decision research: `docs/superpowers/governance/2026-08-14-board-6-code-signing-provider-decision.md`; approved scope amendment: `docs/superpowers/specs/2026-08-14-board-6-private-distribution-profile-design-amendment.md`. Commits `9f4ad0f` and `e9cb67b` make Windows PowerShell Authenticode inspection deterministic under a child-only module environment and extend public signer/timestamper evidence. Commit `50e7074` adds a dormant optional SSL.com eSigner adapter with no provider account/configuration/key/signing side effect. Scope-amendment implementation commit `ebd3378` adds explicit schema-v2 `distribution_profile = private_controlled | public_formal`, preserves schema-v1 production publisher requirements, allows schema-v2 private production with an empty publisher policy, and prevents mutable external launcher config from overriding the distribution profile. Fresh amendment verification passed Python 646 run / 2 expected skips / 0 failures and Worker typecheck + Vitest 92/92; read-only staging trust-profile load remained schema v1 / legacy / staging / stable / empty publisher with no mutation. The current Private / Controlled Distribution profile intentionally keeps `windows_publisher_policy` empty and relies on the mandatory Ed25519 release-manifest + exact package SHA-256/size + server-authoritative release path for remote-update integrity. Real provider/publisher selection, payment/KYC, managed/HSM key provisioning, provider activation, non-empty publisher-policy mutation, and actual signing are **deferred optional Public / Formal Distribution hardening and are not current launch/closure blockers**.
- B6-G7 Staging Key Rotation Drill Gate: **accepted complete**. Evidence: `docs/superpowers/governance/2026-08-14-board-6-key-rotation-preflight.md`. G7-M0 through G7-M5 completed against staging: Admin session HMAC and download-ticket secret overlap/switch/rollback/re-switch ended at current V2/readable V1+V2; contact encryption completed reversible V1→V2→V1→V2 migration with lookup pepper held at V1; live lease signer switched atomically to `lease-key-staging-02`, proved old/new overlap, then atomically restored exact `lease-key-staging-01`; release key-02 was verified local-only against overlap trust while the retained publisher and accepted registry stayed on key-01. Final Worker Version is `633945cb-0103-4fb0-9ccf-7a9c797e6de5`; both disposable G7 licenses are revoked, the G7 device is unbound, all sessions for `admin_board6_g5_primary` are revoked, and that principal is restored to its exact pre-G7 revoked/scopes/revoked_at state. Fresh closure verification passed Python 648 run / 2 expected skips / 0 failures, Worker typecheck, Vitest 93/93, and deployment-preflight 26/26. No V1 retirement, production, push, merge, or release publication occurred.
- B6-G8 Board 6 Closure Gate: **accepted complete**. Final report: `docs/deployment/2026-08-12-board-6-security-delivery-preparation-report.md`. Fresh closure verification passed Python 648 run / 2 expected skips / 0 failures, Worker typecheck, Vitest 93/93, deployment-preflight 26/26, live staging health on both origins, read-only D1/release/admin/G7 reconciliation, immutable main/Board 5 boundary checks, production fail-closed preflight, and tracked sensitive-value scanning. At B6-G8 closure Board 7 was unstarted; B7-G0 design/spec + implementation plan are now prepared locally with no production side effect.
- B6-G3 closure is local/read-only plus governance only. It created no Access/DNS/D1/R2 cloud resource, applied no staging migration, changed no real Secret, deployed no Worker, used no real code-signing identity, ran no production installer, published/registered/enabled no real release, and performed no push/merge. Board 5 evidence cleanup remains separately gated.

Task 3 real staging license/device acceptance succeeded on 2026-08-08. Task 4 is complete: the fresh real staging lease verified under `lease-key-staging-01` with an exact 604800-second duration; valid/expiring/expired and significant-clock-rollback behavior matched policy; and the authorized single-process status sequence `active → suspended → online LICENSE_SUSPENDED → active → revalidate` succeeded against staging. A fresh 2026-08-08 read-only D1 check reconfirmed the license at `active`, revision 3, `suspended_at=NULL`, `revoked_at=NULL`, with 4 historical devices (3 active, 1 unbound) and device 1 still active after post-restore validation. Revocation remains deliberately deferred so the staging license stays usable for later board work.

Task 5 was implemented only in the DevSpace worktree `C:\Users\28276\.devspace\worktrees\wechat-cli-1ec755b9` on branch `task5/0.5.1-update-validation`, based exactly on frozen main HEAD `a579a25`. Commit `84b8a99` provides application 0.5.1, default build ID `staging-051-20260808.1`, first-class app-only Windows build support, update-only packaging, and an update-only artifact-verification path. The resulting local ZIP is `14268929` bytes with SHA-256 `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`; it contains only `wechat-cli.exe` and `app-manifest.json`. A real frozen EXE loopback health probe returned `version=0.5.1` and `build_id=staging-051-20260808.1`. Launcher stayed at 0.1.0, no Launcher EXE or bootstrap was built in the worktree.

Task 6 local signing preparation completed on 2026-08-08 under explicit local-only authorization. `rel_staging_051` was prepared from the verified 0.5.1 ZIP using signing key ID `release-key-staging-01`, channel `stable`, minimum app `0.5.0`, minimum Launcher `0.1.0`, rollout `100`, and signed manifest metadata time `2026-08-08T12:44:04Z`. The manifest SHA-256 is `be111f0a786821319fb96f82f1748b4a75003b7c5fb296c48ec87b3355a3fd62`; the package SHA-256 remains `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`. Independent verification used the corresponding public key from the repo-external Launcher public-key file and confirmed the raw manifest signature, key ID, package hash, ZIP metadata, version/build ID, platform, architecture, and entrypoint. Manifest and signature remain stored outside Git under the restricted staging secrets directory.

On 2026-08-09, after a duplicate/hashing preflight, the separately authorized external publication stage completed. Private GitHub Draft Release `v0.5.1` is Release ID `367353041`, with exactly three uploaded assets: package Asset ID `506974337` (`14268929` bytes), manifest Asset ID `506974359` (`911` bytes), and signature Asset ID `506974373` (`64` bytes). Staging D1 contains exactly `rel_staging_051` with version `0.5.1`, channel `stable`, manifest/package hashes matching the approved local values, package size `14268929`, GitHub repository `AuRevior-ai/wechat-cli-releases`, GitHub Release ID `367353041`, package Asset ID `506974337`, and rollout `100`. The successful `release.register` audit request ID is `08e7af6c-98ac-4e29-9282-7b806f93c0da`. Under a separate enable authorization at 2026-08-09 11:11 +08:00, the Worker record transitioned from `enabled=0`, `paused=1` to `enabled=1`, `paused=0` without changing rollout; the successful `release.update` audit request ID is `626ff686-df79-4281-b1af-18bbabbac434`. Worker `published_at=2026-08-09T01:54:20.521Z` remains the registration timestamp and intentionally differs from the signed manifest metadata timestamp. Post-enable GitHub verification confirmed Draft `v0.5.1` and all three uploaded assets were unchanged.

## Evidence boundary

### Verifiable from this repository

- The 0.5.0 license, launcher, update, release, admin, Worker, and Windows packaging implementations exist.
- The local 0.5.0 update ZIP is `14291197` bytes with SHA-256 `406b72c14ad186141f373087595f7723e143b9638bf298fde23b6bb0ae057523`.
- The current bootstrap archive still contains the Demo API URL and Demo signing-key identifiers and is not a staging installer.
- `wrangler.jsonc` contains the recorded staging Worker, D1, R2, and cron configuration.

### Last manually recorded outside the repository

As recorded in the authorized-update roadmap, the two private repositories and Cloudflare staging resources had been created, and `rel_staging_050` had been uploaded, registered, and enabled. On 2026-08-08 a live D1 read confirmed the Board 4 staging test license: ID `lic_ptrqZVAxh2NI8h5RM6gnGiiL`, hint `JD25`, `stable`, maximum 3 devices, created at `2026-08-08T09:07:57.146Z`. Task 3 then completed against the real staging API: the fourth device was rejected with `DEVICE_LIMIT_REACHED`, rename and unbind/rebind succeeded, and a live D1 read confirmed 4 historical device rows with 3 active and 1 unbound. On 2026-08-10 the separately approved Board 5 stable-license checkpoint created exactly one additional stable license: ID `lic_NGcs-flk8nRqfkbm5TFqewHV`, hint `9C4A`, status `active`, `maximum_devices=1`, audit action `license.batch_create`, request ID `28e0e326-805d-4bff-add0-ab25fcc43400`. The subsequent isolated activation checkpoint bound exactly one active device; Worker readback records app `0.5.0`, Launcher `0.1.0`, and a fresh validation timestamp. On 2026-08-11 the separately approved beta-license gate created exactly one beta license: ID `lic_XUMv4Qor5S1WXr-lWOTd9L1m`, hint `WYW2`, status `active`, `maximum_devices=1`, with zero active devices at creation; audit action `license.batch_create`, request ID `be22ec6c-d222-4a69-bda1-6750c592b4f5`. Its complete key is stored only in the restricted repo-external Board 5 CSV, with adjacent safe metadata. The complete keys and all device tokens remain outside the repository and are not recorded here. These are historical acceptance records, not a substitute for future live checks.

## Last local verification

- Board 5 stable-license + install/activation checkpoint: one stable license exists, ID `lic_NGcs-flk8nRqfkbm5TFqewHV`, hint `9C4A`, status `active`, `maximum_devices=1`; its complete key remains only in the restricted repo-external CSV. The isolated bootstrap was installed with `-NoStart -NoShortcuts -SkipProcessStop -SkipWebView2Check` under the marked `board5-windows-e2e/stable` root; `current.json` is 0.5.0/stable, no shortcut was created, and the 0.5.0 app SHA-256 remains `fe70396252d4cab1cf355e34bb7479233ba2fd1d0ba1866132ab9e0c9f19f971`. Acceptance exposed two independent Launcher UI defects: Windows `file:` URL normalization (`56d065e`) and a pywebview `before_load` deadlock caused by calling the loaded-gated public URL API (`706bcbe`). After the user activated the license, DPAPI state had the expected `WCLIC1` envelope and was not plaintext JSON; license ID matched, lease key ID was `lease-key-staging-01`, lease status was active, and duration was exactly 604800 seconds. Two orphan Task 5 0.5.1 servers on port 18787 were explicitly authorized for termination after PID/path reuse checks; subsequent real health is `0.5.0/dev/ok` with `license_session_valid=true`. Worker readback is `active_devices=1` and one active device at app 0.5.0 / Launcher 0.1.0.
- Board 5 stable Windows E2E acceptance: the approved private-Draft publish gate changed only Release ID `367353041` / `v0.5.1` to `draft=false`, `prerelease=false`, `make_latest=false` and created `refs/tags/v0.5.1` at release-repository commit `2b9fa385b86df83f7968239a1029d4d59f020027`; package/manifest/signature asset IDs, sizes and digests did not change. The acceptance-only range probe (`52e07b8`) then returned HTTP 206, exactly one byte, and `Content-Range: bytes 0-0/14268929`, with package and manifest hashes matching the frozen Board 4 values. A single real update trigger through the running 0.5.0 application's `/api/update/check` launched the installed Launcher; the old 0.5.0 PID remained alive throughout download. `pending-update.json` was produced for `rel_staging_051` / `0.5.1`; the cached ZIP verified at `14268929` bytes and SHA-256 `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`; manifest SHA-256 `be111f0a786821319fb96f82f1748b4a75003b7c5fb296c48ec87b3355a3fd62` verified under `release-key-staging-01`; extracted `wechat-cli.exe` is `14483951` bytes with SHA-256 `dbbdf8ec70ddcf8d36145f099b0cb4ce3292c61e205c3ac7ae4db01fbc8907d1` and reports version `0.5.1`. After exact PID/path/cmd validation, only the old 0.5.0 process was stopped and the installed Launcher was started once with child-only Board 5 environment variables. The formal update transaction reached `committed`; real `/api/health` is `0.5.1` / `staging-051-20260808.1` / `ok` / license session valid; `current.json` records current `0.5.1`, previous `0.5.0`, and the approved manifest SHA; pending state is cleared and both `versions\0.5.0` and `versions\0.5.1` remain. Final read-only reconcile confirms Worker `rel_staging_050/051` values unchanged, GitHub `v0.5.0` still the original Draft, and `v0.5.1` assets unchanged after the authorized publish.
- Board 5 Offline Part A acceptance: a new marked repo-external root `board5-windows-e2e/offline` was created from the successful 0.5.1 stable install/user state while leaving the stable evidence root unchanged. Ephemeral `runtime`/`cache`/`logs` were excluded; the only UserProfile copy anomaly was the Windows `Content.IE5` reparse/system directory, and a post-copy comparison confirmed all 23 non-reparse user files matched with zero missing/extra files. `board5_prepare_launcher_config.py` generated `launcher-config.offline.json` with config SHA-256 `00502225f67e0f4be1fe255942b17ca73ff6e1625fbc0f522ad136fc5a60abcc`; release/lease key registries, channel `stable`, port `18787`, and fingerprint salt matched the copied stable config exactly, while only API authority changed to unused `https://127.0.0.1:65534`. After exact process-tree validation, the stable 0.5.1 PyInstaller parent/child process tree was temporarily stopped to free port 18787. The real OfflineSandbox Launcher then started the copied 0.5.1 app from the OfflineSandbox path; `/api/health` returned `0.5.1` / `staging-051-20260808.1` / `ok` / license session valid and `/api/license` returned `state=offline_valid`, `authorized=true`, current `0.5.1`, previous `0.5.0`, channel `stable`. `board5_offline_acceptance.py` on the same DPAPI state verified lease key `lease-key-staging-01`, exact duration `604800`, valid at `offline_until-1s`, expired at `offline_until+1s`, allowed a 4-minute clock correction, and rejected a 10-minute rollback with `OFFLINE_LEASE_DENIED`; Windows system time was never changed. The OfflineSandbox process tree was then stopped and the original stable Launcher was restarted with the original child-only environment; listener path and health reconfirmed the stable 0.5.1 sandbox is restored and healthy. OfflineSandbox remains preserved as acceptance evidence; no cleanup was performed.
- Board 5 beta-license creation checkpoint: read-only preflight found no existing beta license and no existing target CSV/metadata. One `licenses batch-create` call created exactly one staging beta license, ID `lic_XUMv4Qor5S1WXr-lWOTd9L1m`, hint `WYW2`, `maximum_devices=1`, status `active`, `active_devices=0`. D1 audit `license.batch_create` is `success`, request ID `be22ec6c-d222-4a69-bda1-6750c592b4f5`, metadata count=1 / maximum_devices=1 / release_channel=beta, with the audit readback reporting `rows_written=0`. The complete key exists only in `staging-secrets-20260805/board5/board5-beta-license-01.csv`; adjacent `.metadata.json` contains only safe fields. `icacls` readback confirms both files grant full control only to the current user, SYSTEM, and Administrators.
- Board 5 RollbackSandbox beta alignment checkpoint: a new marked repo-external `board5-windows-e2e/rollback` root was created, and the tested sandbox helper copied the successful stable 0.5.1 install while excluding `runtime/`, `cache/`, and `state/license-state.dat`. Before activation the sandbox preserved current `0.5.1`, previous `0.5.0`, and manifest SHA `be111f0a786821319fb96f82f1748b4a75003b7c5fb296c48ec87b3355a3fd62`; only channel changed from stable to beta through `CurrentVersion` + `InstallLayout.save_current()`. The real RollbackSandbox Launcher activation path created an independent DPAPI license state for beta license hint `WYW2`; real health was `0.5.1` / `staging-051-20260808.1` / `ok` / session valid and `/api/license` reported authorized beta state with current `0.5.1`, previous `0.5.0`. Worker/admin readback confirmed exactly one active device on the beta license with last app `0.5.1` and Launcher `0.1.0`. D1 audits `device.activate` / `device.validate` are success with request IDs `ac579b8c-7d6d-4338-8aa4-6896f56fa21e` / `04539bdc-cc50-4081-bc3a-e7b10793f505`; the read-only query reported `rows_written=0`. No full device ID/token is recorded. After verification the RollbackSandbox process tree was stopped while its independent DPAPI state was preserved; the original stable Launcher was restored and real stable health/session were reverified.
- Board 5 Task 6 Part C local fault preparation checkpoint: approved 0.5.1 EXE bytes (`14483951` bytes, SHA-256 `dbbdf8ec70ddcf8d36145f099b0cb4ce3292c61e205c3ac7ae4db01fbc8907d1`) were copied unchanged into repo-external `wechat-cli-app-0.5.2-board5bad.1-win-x64.zip`. The ZIP is `14268937` bytes with SHA-256 `96111ad0fff5827a93e81d826de150ea616eb4c907b20ada9a420967f8ffaf82`, contains exactly `app-manifest.json` + `wechat-cli.exe`, and the app manifest declares candidate version `0.5.2-board5bad.1` while retaining build ID `staging-051-20260808.1`. Local-only release prepare generated beta release ID `rel_board5_bad_052_01` with signed manifest SHA-256 `2d29da32cb05e1a373ddab58725f2810e1d81489ad769a0ed6094458eb71fdaa`, manifest size `957`, signature size `64`, published_at `2026-08-11T12:41:00Z`, minimum app `0.5.1`, minimum Launcher `0.1.0`, rollout 100, and signing key ID `release-key-staging-01`. Independent verification used only the repo-external staging public-key registry: raw manifest signature verified, package size/hash matched, and target validation for app 0.5.1 / Launcher 0.1.0 passed. Fault package/manifest/signature inherit ACL access only for current user, SYSTEM, and Administrators. No GitHub Release, asset upload, Worker release row, enable, or RollbackSandbox update was performed under this gate. The subsequent read-only publish/register preflight found the release repository clean at `2b9fa385b86df83f7968239a1029d4d59f020027`, GitHub ref `refs/tags/v0.5.2-board5bad.1` absent with explicit 404, D1 `rel_board5_bad_052_01` absent, and `rel_staging_050/051` still at their frozen hash/size/status values with `rows_written=0`.
- Board 5 Task 6 Part C fault publish/register checkpoint: the approved local fault bytes were published exactly once through the existing release publisher without `--enable`. Private GitHub Draft Release ID `368572125` has tag name `v0.5.2-board5bad.1`, `draft=true`, `prerelease=true`, target `main`, and exactly three uploaded assets: package ID `510139118` (14268937 bytes, SHA-256 `96111ad0fff5827a93e81d826de150ea616eb4c907b20ada9a420967f8ffaf82`), manifest ID `510139294` (957 bytes, SHA-256 `2d29da32cb05e1a373ddab58725f2810e1d81489ad769a0ed6094458eb71fdaa`), and signature ID `510139303` (64 bytes, SHA-256 `912b457f0149cdcc0d33ad46f4c937bf2fd6a042e2a13cb5ad5002081ae48a58`). Because the release remains Draft, the actual Git ref `refs/tags/v0.5.2-board5bad.1` still returns 404; only the Draft release's tag_name is recorded. Staging D1 now contains exactly `rel_board5_bad_052_01` with version `0.5.2-board5bad.1`, channel beta, frozen manifest/package hashes and package size, GitHub release/asset mapping above, rollout 100, `enabled=0`, `paused=1`. `release.register` audit result is success with request ID `354aa751-d019-4931-a5f3-23208af084d0`. Post-write read-only reconcile confirmed `rel_staging_050/051` unchanged and all D1 queries reported `rows_written=0`; the release repository remained clean. No enable, RollbackSandbox update, publish-from-Draft, tag creation, production, push, merge, or cleanup occurred.
- Board 5 Task 6 Part C fault-enable checkpoint: after a fresh read-only preflight reconfirmed `rel_board5_bad_052_01` at beta / rollout 100 / `enabled=0` / `paused=1` and `rel_staging_050/051` unchanged, exactly one `wechat-cli-admin releases enable rel_board5_bad_052_01` operation changed only the fault row to `enabled=1`, `paused=0`; rollout remained 100 and all frozen hashes, sizes, and GitHub mapping remained unchanged. D1 `release.update` audit result is success with request ID `315bdfca-04b0-4745-96ee-c545ecd55fd3`, metadata only `enabled=true` / `paused=false`; post-write read-only queries reported `rows_written=0`. GitHub fault Release ID `368572125` remained `draft=true`, `prerelease=true` with the same three asset IDs/sizes/digests, and the actual `refs/tags/v0.5.2-board5bad.1` ref remained absent. Stable `rel_staging_050/051` remained enabled/unpaused at rollout 100. RollbackSandbox was not started; stable sandbox health remained `0.5.1` / `staging-051-20260808.1` / `ok` / license session valid.
- Board 5 Task 6 Part C rollback-acceptance preflight blocker: after explicit rollback-acceptance approval, a read-only production-class beta update check from the preserved RollbackSandbox state selected exactly `rel_board5_bad_052_01` / `0.5.2-board5bad.1` with the frozen package hash/size and no failed versions. Before starting any RollbackSandbox process, a one-byte download request through the real Worker path returned outer HTTP 502 with safe error code `DOWNLOAD_UPSTREAM_FAILED` and GitHub `upstream_status=403`. A control request using the same Worker download path and credential boundary for published `rel_staging_051` returned HTTP 206, `Content-Range: bytes 0-0/14268929`, exactly one byte. This isolated the blocker to the private Draft asset visibility boundary rather than beta selection, license/channel alignment, ticket issuance, Worker routing, or the GitHub read credential generally.
- Board 5 Task 6 Part C fault prerelease + rollback-process checkpoint: under the separately approved visibility gate, Release ID `368572125` was changed only to `draft=false`, `prerelease=true`, `make_latest=false`; real tag `v0.5.2-board5bad.1` was created at release-repository commit `2b9fa385b86df83f7968239a1029d4d59f020027`, while all three existing asset IDs/sizes/digests remained unchanged. The same Worker one-byte probe then returned HTTP 206 / `bytes 0-0/14268937`. A real `--download-update` prepared the frozen fault ZIP and candidate while leaving current at 0.5.1/beta; candidate `wechat-cli.exe --version` still reported 0.5.1. Real `--apply-update` produced transaction `rolled_back`, from 0.5.1 to `0.5.2-board5bad.1`, with failure reason `application health version does not match the update`; current restored to 0.5.1 and failed versions recorded `0.5.2-board5bad.1`. Acceptance nevertheless failed: candidate directory child PID remained as an orphan after its Popen parent exited and continued to own port 18787, while the restored 0.5.1 child could not bind; `/api/health` therefore came from the residual candidate path and had `license_session_valid=false`. Evidence traced the root cause to `ApplicationProcessManager.stop()` terminating only the Popen parent, with no Windows process-tree stop or port-release contract, plus `LauncherService.start()` swallowing candidate-stop errors. The two abnormal RollbackSandbox app processes were then precisely stopped, and stable was restarted through the production Launcher service path; fresh listener/path/health now belongs to stable `versions/0.5.1/wechat-cli.exe` with build `staging-051-20260808.1`, status ok, session valid. No cleanup of the failed RollbackSandbox evidence was performed.
- Board 5 Windows process-tree repair + fresh rollback re-acceptance checkpoint: TDD repair commit `29aba6bc0c8469dc8b5dc512d6831c5385246431` added Windows `taskkill.exe /PID <pid> /T /F` tree termination, bounded loopback port-release verification, and fail-closed rollback orchestration when candidate stop fails. RED tests first demonstrated the missing tree-stop/port-release/stop-failure semantics; GREEN verification passed launcher/update focused 28/28, Windows packaging 31/31, and Python full suite 529 run with 2 expected skips and zero failures. Rebuilt Launcher size/SHA-256=`20168420` / `a9ae3633f96d08880f1ab4a2e45c946a5d9733a9dd2ed12eabb11acf4c1d1ef7`. A new marked repo-external `board5-windows-e2e/rollback-repair` sandbox was created from successful stable 0.5.1, switched to beta, given only the existing beta DPAPI state plus the rebuilt Launcher, and explicitly did not inherit the old failed registry/pending/candidate/transaction state. Fresh beta preflight selected `rel_board5_bad_052_01` with `failed_versions=[]`; one-byte Worker probe returned HTTP 206 / `bytes 0-0/14268937`. Real full download reproduced the frozen fault package/manifest/candidate hashes, then repaired `--apply-update` produced transaction `txn_iHrMsmmUllLYh7u6pp079U2e`, state `rolled_back`, from `0.5.1` to `0.5.2-board5bad.1`, failure reason `application health check timed out: application health version does not match the update`. Post-rollback there were zero candidate-version processes; the sole 18787 listener belonged to repair `versions/0.5.1/wechat-cli.exe`, health was `0.5.1 / staging-051-20260808.1 / ok / license_session_valid=true`, and failed registry key was `0.5.2-board5bad.1|2d29da32cb05e1a373ddab58725f2810e1d81489ad769a0ed6094458eb71fdaa`. A subsequent same-sandbox beta check sent `failed_versions=["0.5.2-board5bad.1"]` and returned `update_available=false` / no manifest, which is **version-level server suppression**; the local registry remains version + manifest hash. Repair processes were then stopped without deleting evidence, and stable was restored; fresh sole listener path belongs to stable `versions/0.5.1/wechat-cli.exe` with `0.5.1 / staging-051-20260808.1 / ok / license_session_valid=true`.
- Board 5 final fault-disable checkpoint: after fresh preflight reconfirmed fault `rel_board5_bad_052_01` at beta / rollout 100 / `enabled=true` / `paused=false`, stable `rel_staging_050/051` unchanged, GitHub fault Release ID `368572125` still private prerelease with unchanged tag/assets, and stable 0.5.1 health/session valid, exactly one Admin API `PATCH /v1/admin/releases/rel_board5_bad_052_01` was issued with `enabled=false` and `paused=true` in the same request. Post-write readback confirmed fault `enabled=false` / `paused=true` / rollout 100; stable 050/051 hashes/status remained unchanged, GitHub prerelease/tag/assets were unchanged, repair sandbox process count was 0, and stable health remained `0.5.1 / staging-051-20260808.1 / ok / license_session_valid=true`. The Admin response does not expose request ID; a separate D1 audit read was not performed because the non-interactive Wrangler shell had no `CLOUDFLARE_API_TOKEN`, and no credential was read or expanded for that purpose.
- Board 5 Task 7 final closure checkpoint: the 2026-08-12 Cloud Cleanup Gate revoked stable `9C4A` and beta `WYW2` to revision 2, then the two associated Board 5 test devices were unbound through the existing Admin CLI. Fresh readback shows both licenses `revoked` with `active_devices=0` and both device rows `unbound`; JD25 remains active/revision 3 with 3 active + 1 unbound historical devices. Fresh post-cleanup verification passed Python 529 run / 2 expected skips / 0 failures, Worker typecheck, Vitest 21/21, Board 4/build-source/hotfix-source/main/release-repo immutable checks, fixed 0.5.0 ZIP size/hash verification, Worker 050/051/fault reconcile, and GitHub v0.5.0/v0.5.1/fault asset/tag reconcile. Local physical cleanup was not executed and remains optional/separately gated. **At the 2026-08-12 Board 5 closure checkpoint, Board 6 remained unstarted; current Board 6 execution state is recorded in the canonical Board 6 section above.**
- Board 5 Bootstrap build gate completion: explicit dependency provisioning installed `pywebview 6.2.1`; WebView2 Runtime was present at `151.0.4129.72`. Detached build-source remained exactly `a579a25cb7f16e6fdf88d618252b4a5cbffef53d`, with `WECHAT_CLI_BUILD_ID` unset. The real app binary reports `wechat-cli, version 0.5.0`; app size/SHA-256 are `14484577` / `fe70396252d4cab1cf355e34bb7479233ba2fd1d0ba1866132ab9e0c9f19f971`, and Launcher size/SHA-256 are `20153234` / `f45d3bb655193e74f433edd38a50dfbdf7b96a3820ad5929a37405d48bb49df1`. Repo-external staging launcher config SHA-256 is `5002d155f3968a0d44a20b070e1b28ce569497b88d9748ba8a606b0201395e9d`, with `stable`, port `18787`, and only `release-key-staging-01` / `lease-key-staging-01` recorded in safe evidence. Bootstrap ZIP size/SHA-256 are `34192856` / `5985fc2e835ab7e45da227f2d62770bc248ecf09525b511fa76e3bf3ae082d8d`; archive app/Launcher/config hashes exactly match their source/package-directory copies. Package manifest is application `0.5.0` with historical package build label `0.5.0-local-20260805.1`; the frozen runtime contract was separately observed as `status=ok`, `product=wechat-cli-web`, `version=0.5.0`, `build_id=dev`. `verify_windows_bootstrap.py` was deliberately not run because it executes install/reinstall/uninstall; this gate allowed only build plus read-only artifact verification. No installer was executed.
- Board 5 Local implementation completion: Task 3 focused suite 18/18 passed; Python full suite 519 tests run with 2 platform skips and no failures; `git diff --cached --check` passed; the four new non-test Board 5 scripts had zero high-risk sensitive-shape matches. Test-only fake license/device-token fixtures are confined to `tests/test_board5_offline_acceptance.py` and are explicitly asserted absent from serialized evidence.
- Task 5 focused version/build/packaging/verifier regression: 39 tests passed before the final full-suite run; current Admin/License User-Agent contract regression: 17 tests passed.
- Python full suite after Task 5: 489 tests run; 487 passed and 2 skipped.
- Worker dependency restoration in the isolated worktree used the tracked `services/license-update-worker/package-lock.json` (`lockfileVersion=3`) with exactly `npm ci --ignore-scripts --no-audit --no-fund`; `package.json` and `package-lock.json` remained unchanged. Earlier wording that the Worker directory had no lockfile was incorrect and is superseded by this verified record.
- Worker: `npm run typecheck` passed; Vitest: 3 files passed, 17 tests passed using `npm test -- --run`.
- Real app-only PyInstaller build succeeded with PyInstaller 6.20.0. Existing test output mentioning missing `pywebview` comes from the intentional legacy full-build preflight test, not the Task 5 build path.
- Update-only verifier passed ephemeral Ed25519 signing/verification, SHA-256 verification, safe extraction, and execution of the extracted EXE `--version` (`0.5.1`).
- Real EXE loopback `/api/health` returned `version=0.5.1`, `build_id=staging-051-20260808.1`, `status=ok`; `license_session_valid=false` was expected because the probe intentionally ran without a Launcher session.
- 0.5.1 update ZIP: size `14268929`, SHA-256 `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`; members exactly `app-manifest.json` and `wechat-cli.exe`.
- `git diff --check` passed and sensitive-value shape scans over the Task 5 implementation/tests found zero matches. Build output, `dist/`, Worker `node_modules/`, and platform EXEs remain ignored.
- Task 7 fresh verification on 2026-08-09: Python 489 run / 487 passed / 2 skipped; Worker typecheck passed; Vitest 17/17 passed; update-only artifact verifier passed with ZIP size `14268929` and SHA-256 `0ddbb0b402cc9d98ed001391c1a618527d165e328a1f4f1534d80e6b984956c0`.
- Final live read-only D1 verification confirmed the staging license remains active, device history remains 3 active + 1 unbound, and both `rel_staging_050` and `rel_staging_051` are `enabled=1`, `paused=0`, rollout 100; all D1 final queries reported `changes=0`, `changed_db=false`, and `rows_written=0`.
- Final GitHub verification confirmed both private Draft releases `v0.5.0` and `v0.5.1` retain exactly three uploaded package/manifest/signature assets with the recorded IDs, names, and sizes.
- `release.register` is `success` with request ID `08e7af6c-98ac-4e29-9282-7b806f93c0da`; the separate `release.update` enable audit is `success` with request ID `626ff686-df79-4281-b1af-18bbabbac434`.
- Non-test tracked source/docs and the prepared 0.5.1 asset directory had zero target sensitive-shape matches; four broad GitHub-token-shaped matches were confined to unit-test fixture files, with zero private-key block and device-token/device-ID long-shape matches.
- Board 4 acceptance report: `docs/deployment/2026-08-09-board-4-test-license-and-release-report.md`.
- Verification date: 2026-08-09.

## Known constraints

- Board 4 is accepted complete and frozen at `task5/0.5.1-update-validation@8c7464f`. Board 5 is **accepted complete**, including stable 0.5.0→0.5.1 update, offline acceptance, beta fault lifecycle, repaired real rollback, version-level suppression, final fault disable, Cloud Cleanup Gate completion, and fresh final verification. Both Board 5 stable/beta licenses are revoked with zero active devices and their associated test-device rows are unbound. The stable sandbox remains retained as evidence at 0.5.1 with previous version 0.5.0; the fault release remains disabled/paused at rollout 100. Local physical cleanup remains optional and separately gated. Board 6 is **accepted complete** under the Private / Controlled Distribution profile; B6-G0 is complete at `c1d0458`, B6-G1 is complete at `bdc98af`, B6-G2 is complete at `e0c91df`, B6-G3 is complete through `1a07447` with audit `d73cf3f`, B6-G4 is complete with staging infrastructure report `docs/superpowers/governance/2026-08-14-board-6-staging-infrastructure-gate.md`, B6-G5 is accepted complete with report `docs/superpowers/governance/2026-08-14-board-6-staging-behavior-acceptance.md`, and B6-G6 **Phase A** is complete while real commercial Authenticode is deferred by the approved Private / Controlled Distribution scope. B6-G7 is accepted complete after the exact G7-M0..M5 staging drill and cleanup; B6-G8 final closure is accepted complete with the final acceptance report above. Board 7 B7-G0/B7-G1 are complete, and the primary Board 7 `0.6.0` / Launcher `0.2.0` lineage has already been history-preserving merged through PR #1 into canonical remote `main=f63eb76b596080cb68b2285a8d0b0cc8e413a9a4`. B7-G2 remains in governance cleanup only because the post-merge CI exposed two historical whitespace lines; exact repair `9185f5f...` is pushed pending PR/CI/merge, and the approved Strong Authorization amendment must then be integrated before B7-G3. Production remains fail-closed/unprovisioned/undeployed.
- Task 4 real staging lease, clock-boundary, and suspend/reject/restore behavior are accepted. Revocation remains deferred to avoid destroying the test license before later board work.
- Task 5 implementation remains isolated from frozen `main`; merge/push are not authorized. The main checkout’s fixed 0.5.0 ZIP was not present inside the managed worktree because `dist/` is ignored, so the Task 5 build path could not overwrite that main-checkout artifact. The frozen 0.5.0 reference remains size `14291197`, SHA-256 `406b72c14ad186141f373087595f7723e143b9638bf298fde23b6bb0ae057523`.
- The main checkout currently contains an intentionally preserved untracked `NUL` entry; it must not be committed or deleted without separate explicit approval.
- The repository/default 0.5.0 bootstrap remains Demo-configured and must not be used for staging acceptance. The separately generated repo-external Board 5 staging bootstrap is installed only in the marked isolated stable sandbox; its Launcher carries the two local acceptance hotfixes above, while the 0.5.0 app binary remains unchanged.
- Windows executables are not code-signed. This is an explicitly accepted limitation of the current Private / Controlled Distribution profile; commercial Authenticode remains deferred optional hardening for future Public / Formal Distribution.
- Production D1 configuration still contains a replacement placeholder.
- npm package metadata remains at `0.2.4`; the Python/Windows Task 5 branch is `0.5.1`, while frozen main remains unmerged at its pre-Task-5 state.

## Authoritative links

- [Authorized update roadmap](deployment/authorized-update-roadmap.md)
- [Current board 4 plan](superpowers/plans/2026-08-05-board-4-test-license-and-release.md)
- [External-memory governance design](superpowers/specs/2026-08-07-external-memory-governance-design.md)
- [0.5.0 local finalization report](deployment/2026-08-05-local-finalization-report.md)
- [Board 4 acceptance report](deployment/2026-08-09-board-4-test-license-and-release-report.md)
- [Board 5 Windows staging E2E design](superpowers/specs/2026-08-09-board-5-windows-staging-e2e-design.md)
- [Board 5 Windows staging E2E implementation plan](superpowers/plans/2026-08-09-board-5-windows-staging-e2e.md)
- [Board 5 Windows staging E2E final acceptance report](deployment/2026-08-12-board-5-windows-e2e-report.md)
- [Board 5 process-tree rollback repair design](superpowers/specs/2026-08-12-board5-windows-process-tree-rollback-repair-design.md)
- [Board 5 process-tree rollback repair plan](superpowers/plans/2026-08-12-board5-windows-process-tree-rollback-repair.md)
- [Board 6 source-integration provenance](superpowers/governance/2026-08-12-board-6-source-integration-provenance.md)
- [Board 6 Private / Controlled Distribution design amendment](superpowers/specs/2026-08-14-board-6-private-distribution-profile-design-amendment.md)
- [Board 6 Private / Controlled Distribution plan amendment](superpowers/plans/2026-08-14-board-6-private-distribution-profile-amendment.md)
- [Board 7 Production Automation & Controlled Launch design](superpowers/specs/2026-08-15-board-7-production-automation-controlled-launch-design.md)
- [Board 7 Production Automation & Controlled Launch implementation plan](superpowers/plans/2026-08-15-board-7-production-automation-controlled-launch.md)
- [Board 6 pre-staging security audit](superpowers/governance/2026-08-13-board-6-pre-staging-security-audit.md)
- [Board 6 staging infrastructure gate](superpowers/governance/2026-08-14-board-6-staging-infrastructure-gate.md)
- [Board 6 staging behavior acceptance](superpowers/governance/2026-08-14-board-6-staging-behavior-acceptance.md)
- [Board 6 code-signing entry readiness audit](superpowers/governance/2026-08-14-board-6-code-signing-entry-readiness.md)
- [Board 6 retained code-signing provider research](superpowers/governance/2026-08-14-board-6-code-signing-provider-decision.md)
- [Board 6 approved security & delivery design](superpowers/specs/2026-08-12-board-6-security-delivery-preparation-design.md)
- [Board 6 approved implementation plan](superpowers/plans/2026-08-12-board-6-security-delivery-preparation.md)
- [Changelog](../CHANGELOG.md)
